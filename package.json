{
  "name": "zerodha-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm start --prefix backend\" \"cross-env PORT=3000 npm start --prefix frontend\" \"cross-env PORT=3001 BROWSER=none npm start --prefix dashboard\"",
    "build": "npm run build --prefix frontend && npm run build --prefix dashboard",
    "start": "node backend/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1"
  }
}
